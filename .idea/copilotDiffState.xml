<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="$PROJECT_DIR$/jet2holiday/src/main/java/org/group/jet2holiday/client/LlmAdviceClient.java">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/jet2holiday/src/main/java/org/group/jet2holiday/client/LlmAdviceClient.java" />
              <option name="updatedContent" value="package org.group.jet2holiday.client;&#10;&#10;public interface LlmAdviceClient {&#10;&#10;    LlmAdviceResult generateAdvice(String prompt);&#10;&#10;    String modelName();&#10;}&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/jet2holiday/src/test/java/org/group/jet2holiday/controller/AccountControllerTest.java">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/jet2holiday/src/test/java/org/group/jet2holiday/controller/AccountControllerTest.java" />
              <option name="originalContent" value="package org.group.jet2holiday.controller;&#10;&#10;import java.math.BigDecimal;&#10;import java.util.HashMap;&#10;import java.util.Map;&#10;import java.util.Optional;&#10;import org.group.jet2holiday.entity.Account;&#10;import org.group.jet2holiday.repository.AccountRepository;&#10;import org.junit.jupiter.api.BeforeEach;&#10;import org.junit.jupiter.api.Test;&#10;import org.mockito.InjectMocks;&#10;import org.mockito.Mock;&#10;import org.mockito.MockitoAnnotations;&#10;&#10;import static org.junit.jupiter.api.Assertions.*;&#10;import static org.mockito.Mockito.*;&#10;&#10;class AccountControllerTest {&#10;&#10;    @Mock&#10;    private AccountRepository accountRepository;&#10;&#10;    @InjectMocks&#10;    private AccountController accountController;&#10;&#10;    private Account testAccount;&#10;&#10;    @BeforeEach&#10;    void setUp() {&#10;        MockitoAnnotations.openMocks(this);&#10;        &#10;        // 初始化测试数据&#10;        testAccount = new Account();&#10;        testAccount.setId(1L);&#10;        testAccount.setAccountName(&quot;Default Portfolio Account&quot;);&#10;        testAccount.setCashBalance(BigDecimal.valueOf(1000.00));&#10;        testAccount.setCurrency(&quot;USD&quot;);&#10;    }&#10;&#10;    @Test&#10;    void getAccount() {&#10;        // 模拟 repository 行为&#10;        when(accountRepository.findById(1L)).thenReturn(Optional.of(testAccount));&#10;        &#10;        // 执行测试&#10;        Account result = accountController.getAccount(1L);&#10;        &#10;        // 验证结果&#10;        assertNotNull(result);&#10;        assertEquals(testAccount.getId(), result.getId());&#10;        assertEquals(testAccount.getAccountName(), result.getAccountName());&#10;        &#10;        // 验证方法调用&#10;        verify(accountRepository, times(1)).findById(1L);&#10;    }&#10;&#10;    @Test&#10;    void getAccount_NotFound() {&#10;        // 模拟 repository 行为&#10;        when(accountRepository.findById(1L)).thenReturn(Optional.empty());&#10;        &#10;        // 执行测试并验证异常&#10;        assertThrows(RuntimeException.class, () -&gt; accountController.getAccount(1L));&#10;        &#10;        // 验证方法调用&#10;        verify(accountRepository, times(1)).findById(1L);&#10;    }&#10;&#10;&#10;    @Test&#10;    void updateBalance_NotFound() {&#10;        // 准备请求数据&#10;        Map&lt;String, Object&gt; requestBody = new HashMap&lt;&gt;();&#10;        requestBody.put(&quot;amount&quot;, &quot;2000.00&quot;);&#10;        &#10;        // 模拟 repository 行为&#10;        when(accountRepository.findById(1L)).thenReturn(Optional.empty());&#10;        &#10;        // 执行测试并验证异常&#10;        assertThrows(RuntimeException.class, () -&gt; accountController.updateBalance(1L, requestBody));&#10;        &#10;        // 验证方法调用&#10;        verify(accountRepository, times(1)).findById(1L);&#10;        verify(accountRepository, never()).save(any(Account.class));&#10;    }&#10;}" />
              <option name="updatedContent" value="package org.group.jet2holiday.controller;&#10;&#10;import java.math.BigDecimal;&#10;import java.util.Optional;&#10;import org.group.jet2holiday.dto.account.AccountResponse;&#10;import org.group.jet2holiday.dto.account.UpdateCashBalanceRequest;&#10;import org.group.jet2holiday.dto.account.UpdateCashBalanceResponse;&#10;import org.group.jet2holiday.entity.Account;&#10;import org.group.jet2holiday.exception.ResourceNotFoundException;&#10;import org.group.jet2holiday.repository.AccountRepository;&#10;import org.junit.jupiter.api.BeforeEach;&#10;import org.junit.jupiter.api.Test;&#10;import org.mockito.InjectMocks;&#10;import org.mockito.Mock;&#10;import org.mockito.MockitoAnnotations;&#10;&#10;import static org.junit.jupiter.api.Assertions.assertEquals;&#10;import static org.junit.jupiter.api.Assertions.assertNotNull;&#10;import static org.junit.jupiter.api.Assertions.assertThrows;&#10;import static org.mockito.ArgumentMatchers.any;&#10;import static org.mockito.Mockito.never;&#10;import static org.mockito.Mockito.times;&#10;import static org.mockito.Mockito.verify;&#10;import static org.mockito.Mockito.when;&#10;&#10;class AccountControllerTest {&#10;&#10;    @Mock&#10;    private AccountRepository accountRepository;&#10;&#10;    @InjectMocks&#10;    private AccountController accountController;&#10;&#10;    private Account testAccount;&#10;&#10;    @BeforeEach&#10;    void setUp() {&#10;        MockitoAnnotations.openMocks(this);&#10;&#10;        testAccount = new Account();&#10;        testAccount.setId(1L);&#10;        testAccount.setAccountName(&quot;Default Portfolio Account&quot;);&#10;        testAccount.setCashBalance(BigDecimal.valueOf(1000.00));&#10;        testAccount.setCurrency(&quot;USD&quot;);&#10;    }&#10;&#10;    @Test&#10;    void getAccount() {&#10;        when(accountRepository.findById(1L)).thenReturn(Optional.of(testAccount));&#10;&#10;        AccountResponse result = accountController.getAccount(1L);&#10;&#10;        assertNotNull(result);&#10;        assertEquals(testAccount.getId(), result.getId());&#10;        assertEquals(testAccount.getAccountName(), result.getAccountName());&#10;        verify(accountRepository, times(1)).findById(1L);&#10;    }&#10;&#10;    @Test&#10;    void getAccount_NotFound() {&#10;        when(accountRepository.findById(1L)).thenReturn(Optional.empty());&#10;&#10;        assertThrows(ResourceNotFoundException.class, () -&gt; accountController.getAccount(1L));&#10;        verify(accountRepository, times(1)).findById(1L);&#10;    }&#10;&#10;    @Test&#10;    void updateBalance() {&#10;        UpdateCashBalanceRequest request = new UpdateCashBalanceRequest();&#10;        request.setAmount(new BigDecimal(&quot;2000.00&quot;));&#10;        when(accountRepository.findById(1L)).thenReturn(Optional.of(testAccount));&#10;        when(accountRepository.save(any(Account.class))).thenReturn(testAccount);&#10;&#10;        UpdateCashBalanceResponse response = accountController.updateBalance(1L, request);&#10;&#10;        assertNotNull(response);&#10;        assertEquals(1L, response.getAccountId());&#10;        assertEquals(new BigDecimal(&quot;2000.00&quot;), response.getCashBalance());&#10;        verify(accountRepository, times(1)).save(any(Account.class));&#10;    }&#10;&#10;    @Test&#10;    void updateBalance_NotFound() {&#10;        UpdateCashBalanceRequest request = new UpdateCashBalanceRequest();&#10;        request.setAmount(new BigDecimal(&quot;2000.00&quot;));&#10;        when(accountRepository.findById(1L)).thenReturn(Optional.empty());&#10;&#10;        assertThrows(ResourceNotFoundException.class, () -&gt; accountController.updateBalance(1L, request));&#10;&#10;        verify(accountRepository, times(1)).findById(1L);&#10;        verify(accountRepository, never()).save(any(Account.class));&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/jet2holiday/src/test/java/org/group/jet2holiday/controller/PortfolioControllerTest.java">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/jet2holiday/src/test/java/org/group/jet2holiday/controller/PortfolioControllerTest.java" />
              <option name="originalContent" value="package org.group.jet2holiday.controller;&#10;&#10;import java.math.BigDecimal;&#10;import java.util.Collections;&#10;import java.util.List;&#10;import java.util.Optional;&#10;import org.group.jet2holiday.entity.Account;&#10;import org.group.jet2holiday.entity.PortfolioItem;&#10;import org.group.jet2holiday.exception.ResourceNotFoundException;&#10;import org.group.jet2holiday.repository.AccountRepository;&#10;import org.group.jet2holiday.repository.PortfolioItemRepository;&#10;import org.junit.jupiter.api.BeforeEach;&#10;import org.junit.jupiter.api.Test;&#10;import org.mockito.InjectMocks;&#10;import org.mockito.Mock;&#10;import org.mockito.MockitoAnnotations;&#10;&#10;import static org.junit.jupiter.api.Assertions.*;&#10;import static org.mockito.Mockito.*;&#10;&#10;class PortfolioControllerTest {&#10;&#10;    @Mock&#10;    private PortfolioItemRepository portfolioItemRepository;&#10;&#10;    @Mock&#10;    private AccountRepository accountRepository;&#10;&#10;    @InjectMocks&#10;    private PortfolioController portfolioController;&#10;&#10;    private Account testAccount;&#10;    private PortfolioItem testPortfolioItem;&#10;    private PortfolioController.PortfolioItemRequest testRequest;&#10;&#10;    @BeforeEach&#10;    void setUp() {&#10;        MockitoAnnotations.openMocks(this);&#10;&#10;        testAccount = new Account();&#10;        testAccount.setId(1L);&#10;        &#10;        testPortfolioItem = new PortfolioItem();&#10;        testPortfolioItem.setId(1L);&#10;        testPortfolioItem.setAccount(testAccount);&#10;        testPortfolioItem.setSymbol(&quot;AAPL&quot;);&#10;        testPortfolioItem.setCompanyName(&quot;Apple Inc.&quot;);&#10;        testPortfolioItem.setAssetType(&quot;STOCK&quot;);&#10;        testPortfolioItem.setShares(BigDecimal.valueOf(10));&#10;        testPortfolioItem.setCostBasis(BigDecimal.valueOf(150.00));&#10;        testPortfolioItem.setCurrency(&quot;USD&quot;);&#10;        &#10;        testRequest = new PortfolioController.PortfolioItemRequest(&#10;                &quot;AAPL&quot;,&#10;                &quot;Apple Inc.&quot;,&#10;                &quot;STOCK&quot;,&#10;                BigDecimal.valueOf(10),&#10;                BigDecimal.valueOf(150.00),&#10;                &quot;USD&quot;&#10;        );&#10;        &#10;        // simulate getCurrentAccount() behavior&#10;        when(accountRepository.findAll()).thenReturn(Collections.singletonList(testAccount));&#10;    }&#10;&#10;    @Test&#10;    void getPortfolioItems() {&#10;        // simulate repository behavior&#10;        when(portfolioItemRepository.findByAccountId(testAccount.getId()))&#10;                .thenReturn(Collections.singletonList(testPortfolioItem));&#10;        &#10;&#10;        List&lt;PortfolioController.PortfolioItemResponse&gt; response = portfolioController.getPortfolioItems();&#10;&#10;        // verify response&#10;        assertNotNull(response);&#10;        assertEquals(1, response.size());&#10;        assertEquals(testPortfolioItem.getSymbol(), response.get(0).symbol());&#10;        &#10;        // verify method call&#10;        verify(portfolioItemRepository, times(1)).findByAccountId(testAccount.getId());&#10;    }&#10;&#10;    @Test&#10;    void getPortfolioItem() {&#10;&#10;        when(portfolioItemRepository.findById(1L)).thenReturn(Optional.of(testPortfolioItem));&#10;        &#10;&#10;        PortfolioController.PortfolioItemResponse response = portfolioController.getPortfolioItem(1L);&#10;        &#10;        // verify response&#10;        assertNotNull(response);&#10;        assertEquals(testPortfolioItem.getSymbol(), response.symbol());&#10;        verify(portfolioItemRepository, times(1)).findById(1L);&#10;    }&#10;&#10;    @Test&#10;    void getPortfolioItem_NotFound() {&#10;        // 模拟 repository 行为&#10;        when(portfolioItemRepository.findById(1L)).thenReturn(Optional.empty());&#10;        &#10;        // 执行测试并验证异常&#10;        assertThrows(ResourceNotFoundException.class, () -&gt; portfolioController.getPortfolioItem(1L));&#10;        &#10;        // 验证方法调用&#10;        verify(portfolioItemRepository, times(1)).findById(1L);&#10;    }&#10;&#10;    @Test&#10;    void createPortfolioItem() {&#10;&#10;        // simulate repository behavior&#10;        when(portfolioItemRepository.save(any(PortfolioItem.class))).thenReturn(testPortfolioItem);&#10;        &#10;        // 执行测试&#10;        PortfolioController.PortfolioItemResponse response = portfolioController.createPortfolioItem(testRequest);&#10;        &#10;        // 验证结果&#10;        assertNotNull(response);&#10;        assertEquals(testRequest.symbol(), response.symbol());&#10;        &#10;        // 验证方法调用&#10;        verify(portfolioItemRepository, times(1)).save(any(PortfolioItem.class));&#10;    }&#10;&#10;    @Test&#10;    void updatePortfolioItem() {&#10;        // 模拟 repository 行为&#10;        when(portfolioItemRepository.findById(1L)).thenReturn(Optional.of(testPortfolioItem));&#10;        when(portfolioItemRepository.save(any(PortfolioItem.class))).thenReturn(testPortfolioItem);&#10;        &#10;        // 执行测试&#10;        PortfolioController.PortfolioItemResponse response = portfolioController.updatePortfolioItem(1L, testRequest);&#10;        &#10;        // 验证结果&#10;        assertNotNull(response);&#10;        assertEquals(testRequest.symbol(), response.symbol());&#10;        &#10;        // 验证方法调用&#10;        verify(portfolioItemRepository, times(1)).findById(1L);&#10;        verify(portfolioItemRepository, times(1)).save(any(PortfolioItem.class));&#10;    }&#10;&#10;    @Test&#10;    void updatePortfolioItem_NotFound() {&#10;        // 模拟 repository 行为&#10;        when(portfolioItemRepository.findById(1L)).thenReturn(Optional.empty());&#10;        &#10;        // 执行测试并验证异常&#10;        assertThrows(ResourceNotFoundException.class, () -&gt; portfolioController.updatePortfolioItem(1L, testRequest));&#10;        &#10;        // 验证方法调用&#10;        verify(portfolioItemRepository, times(1)).findById(1L);&#10;        verify(portfolioItemRepository, never()).save(any(PortfolioItem.class));&#10;    }&#10;&#10;    @Test&#10;    void deletePortfolioItem() {&#10;        // 模拟 repository 行为&#10;        when(portfolioItemRepository.findById(1L)).thenReturn(Optional.of(testPortfolioItem));&#10;        doNothing().when(portfolioItemRepository).delete(testPortfolioItem);&#10;        &#10;        // 执行测试&#10;        portfolioController.deletePortfolioItem(1L);&#10;        &#10;        // 验证方法调用&#10;        verify(portfolioItemRepository, times(1)).findById(1L);&#10;        verify(portfolioItemRepository, times(1)).delete(testPortfolioItem);&#10;    }&#10;&#10;    @Test&#10;    void deletePortfolioItem_NotFound() {&#10;        // 模拟 repository 行为&#10;        when(portfolioItemRepository.findById(1L)).thenReturn(Optional.empty());&#10;        &#10;        // 执行测试并验证异常&#10;        assertThrows(ResourceNotFoundException.class, () -&gt; portfolioController.deletePortfolioItem(1L));&#10;        &#10;        // 验证方法调用&#10;        verify(portfolioItemRepository, times(1)).findById(1L);&#10;        verify(portfolioItemRepository, never()).delete(any(PortfolioItem.class));&#10;    }&#10;}" />
              <option name="updatedContent" value="package org.group.jet2holiday.controller;&#10;&#10;import java.math.BigDecimal;&#10;import java.util.Collections;&#10;import java.util.List;&#10;import java.util.Optional;&#10;import org.group.jet2holiday.dto.portfolio.PortfolioItemRequest;&#10;import org.group.jet2holiday.dto.portfolio.PortfolioItemResponse;&#10;import org.group.jet2holiday.entity.Account;&#10;import org.group.jet2holiday.entity.PortfolioItem;&#10;import org.group.jet2holiday.exception.ResourceNotFoundException;&#10;import org.group.jet2holiday.repository.AccountRepository;&#10;import org.group.jet2holiday.repository.PortfolioItemRepository;&#10;import org.junit.jupiter.api.BeforeEach;&#10;import org.junit.jupiter.api.Test;&#10;import org.mockito.InjectMocks;&#10;import org.mockito.Mock;&#10;import org.mockito.MockitoAnnotations;&#10;&#10;import static org.junit.jupiter.api.Assertions.assertEquals;&#10;import static org.junit.jupiter.api.Assertions.assertNotNull;&#10;import static org.junit.jupiter.api.Assertions.assertThrows;&#10;import static org.mockito.Mockito.any;&#10;import static org.mockito.Mockito.doNothing;&#10;import static org.mockito.Mockito.never;&#10;import static org.mockito.Mockito.times;&#10;import static org.mockito.Mockito.verify;&#10;import static org.mockito.Mockito.when;&#10;&#10;class PortfolioControllerTest {&#10;&#10;    @Mock&#10;    private PortfolioItemRepository portfolioItemRepository;&#10;&#10;    @Mock&#10;    private AccountRepository accountRepository;&#10;&#10;    @InjectMocks&#10;    private PortfolioController portfolioController;&#10;&#10;    private Account testAccount;&#10;    private PortfolioItem testPortfolioItem;&#10;    private PortfolioItemRequest testRequest;&#10;&#10;    @BeforeEach&#10;    void setUp() {&#10;        MockitoAnnotations.openMocks(this);&#10;&#10;        testAccount = new Account();&#10;        testAccount.setId(1L);&#10;&#10;        testPortfolioItem = new PortfolioItem();&#10;        testPortfolioItem.setId(1L);&#10;        testPortfolioItem.setAccount(testAccount);&#10;        testPortfolioItem.setSymbol(&quot;AAPL&quot;);&#10;        testPortfolioItem.setCompanyName(&quot;Apple Inc.&quot;);&#10;        testPortfolioItem.setAssetType(&quot;STOCK&quot;);&#10;        testPortfolioItem.setShares(BigDecimal.valueOf(10));&#10;        testPortfolioItem.setCostBasis(BigDecimal.valueOf(150.00));&#10;        testPortfolioItem.setCurrency(&quot;USD&quot;);&#10;&#10;        testRequest = new PortfolioItemRequest(&#10;                &quot;AAPL&quot;,&#10;                &quot;Apple Inc.&quot;,&#10;                &quot;STOCK&quot;,&#10;                BigDecimal.valueOf(10),&#10;                BigDecimal.valueOf(150.00),&#10;                &quot;USD&quot;&#10;        );&#10;&#10;        when(accountRepository.findAll()).thenReturn(Collections.singletonList(testAccount));&#10;    }&#10;&#10;    @Test&#10;    void getPortfolioItems() {&#10;        when(portfolioItemRepository.findByAccountId(testAccount.getId()))&#10;                .thenReturn(Collections.singletonList(testPortfolioItem));&#10;&#10;        List&lt;PortfolioItemResponse&gt; response = portfolioController.getPortfolioItems();&#10;&#10;        assertNotNull(response);&#10;        assertEquals(1, response.size());&#10;        assertEquals(testPortfolioItem.getSymbol(), response.get(0).getSymbol());&#10;        verify(portfolioItemRepository, times(1)).findByAccountId(testAccount.getId());&#10;    }&#10;&#10;    @Test&#10;    void getPortfolioItem() {&#10;        when(portfolioItemRepository.findById(1L)).thenReturn(Optional.of(testPortfolioItem));&#10;&#10;        PortfolioItemResponse response = portfolioController.getPortfolioItem(1L);&#10;&#10;        assertNotNull(response);&#10;        assertEquals(testPortfolioItem.getSymbol(), response.getSymbol());&#10;        verify(portfolioItemRepository, times(1)).findById(1L);&#10;    }&#10;&#10;    @Test&#10;    void getPortfolioItem_NotFound() {&#10;        when(portfolioItemRepository.findById(1L)).thenReturn(Optional.empty());&#10;&#10;        assertThrows(ResourceNotFoundException.class, () -&gt; portfolioController.getPortfolioItem(1L));&#10;        verify(portfolioItemRepository, times(1)).findById(1L);&#10;    }&#10;&#10;    @Test&#10;    void createPortfolioItem() {&#10;        when(portfolioItemRepository.save(any(PortfolioItem.class))).thenReturn(testPortfolioItem);&#10;&#10;        PortfolioItemResponse response = portfolioController.createPortfolioItem(testRequest);&#10;&#10;        assertNotNull(response);&#10;        assertEquals(testRequest.getSymbol(), response.getSymbol());&#10;        verify(portfolioItemRepository, times(1)).save(any(PortfolioItem.class));&#10;    }&#10;&#10;    @Test&#10;    void updatePortfolioItem() {&#10;        when(portfolioItemRepository.findById(1L)).thenReturn(Optional.of(testPortfolioItem));&#10;        when(portfolioItemRepository.save(any(PortfolioItem.class))).thenReturn(testPortfolioItem);&#10;&#10;        PortfolioItemResponse response = portfolioController.updatePortfolioItem(1L, testRequest);&#10;&#10;        assertNotNull(response);&#10;        assertEquals(testRequest.getSymbol(), response.getSymbol());&#10;        verify(portfolioItemRepository, times(1)).findById(1L);&#10;        verify(portfolioItemRepository, times(1)).save(any(PortfolioItem.class));&#10;    }&#10;&#10;    @Test&#10;    void updatePortfolioItem_NotFound() {&#10;        when(portfolioItemRepository.findById(1L)).thenReturn(Optional.empty());&#10;&#10;        assertThrows(ResourceNotFoundException.class, () -&gt; portfolioController.updatePortfolioItem(1L, testRequest));&#10;&#10;        verify(portfolioItemRepository, times(1)).findById(1L);&#10;        verify(portfolioItemRepository, never()).save(any(PortfolioItem.class));&#10;    }&#10;&#10;    @Test&#10;    void deletePortfolioItem() {&#10;        when(portfolioItemRepository.findById(1L)).thenReturn(Optional.of(testPortfolioItem));&#10;        doNothing().when(portfolioItemRepository).delete(testPortfolioItem);&#10;&#10;        portfolioController.deletePortfolioItem(1L);&#10;&#10;        verify(portfolioItemRepository, times(1)).findById(1L);&#10;        verify(portfolioItemRepository, times(1)).delete(testPortfolioItem);&#10;    }&#10;&#10;    @Test&#10;    void deletePortfolioItem_NotFound() {&#10;        when(portfolioItemRepository.findById(1L)).thenReturn(Optional.empty());&#10;&#10;        assertThrows(ResourceNotFoundException.class, () -&gt; portfolioController.deletePortfolioItem(1L));&#10;&#10;        verify(portfolioItemRepository, times(1)).findById(1L);&#10;        verify(portfolioItemRepository, never()).delete(any(PortfolioItem.class));&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>